@model RestaurantRater_MVC.Models.Restaurant

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Restaurant</h4>
    <hr />
    <dl class="dl-horizontal">        
        <dl>
            <dt>@Html.DisplayNameFor(model=>model.Name)</dt>
            <dd>@Html.DisplayFor(m=>m.Name)</dd>
            <dt>@Html.DisplayNameFor(model => model.Address)</dt>
            <dd>@Html.DisplayFor(m => m.Address)</dd>
            <dt>@Html.DisplayNameFor(model => model.Rating)</dt>
            <dd>@Html.DisplayFor(m => m.Rating)</dd>            
        </dl>
    </dl>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" name="Delete" value="btn btn-default" />
            @Html.ActionLink("Back to List", "Index")
        </div>   
    }
</div>
